<!DOCTYPE html>
<html>
<head>
  <title>ChoreCorps</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <link href="https://fonts.googleapis.com/css?family=Coming+Soon|Antic+Slab" rel="stylesheet">
</head>
<body>
  <header>
    <h1>ChoreCorps</h1>
    <nav>
    <% if signed_in? %>
    <%= current_user.name + " " + current_family.name %> |
    <%= link_to "Me", user_path(current_family, current_user)%> |
    <%= link_to "My Family", family_users_path(current_family) %> |
    <%= link_to "Our Chores", family_path(current_family) %> |
    <%= link_to "Add Member with Google", user_google_oauth2_omniauth_authorize_path %> |
    <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
    <% else %>
    Hi, Stranger! |
    <%= link_to "Sign Up with a New Family", new_family_path %> |
    <%= link_to "Sign Up with an Existing Family", new_user_registration_path %> |
    <%= link_to "Sign In", new_user_session_path %>
    <% end %>
    </nav>
  </header>
  <main>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

  <%= yield %>
  </main>
</body>
</html>
